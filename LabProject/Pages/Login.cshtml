@page
@model LabProject.Pages.LoginModel
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Login</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <style>
        body {
            background-color: #000000;
            color: white;
        }

        .login-box {
            width: 320px;
            margin-top: 100px;
        }

        .form-control {
            height: 35px;
        }

        .btn-gs {
            background-color: #FDB913;
            color: black;
            border: none;
        }

        .btn-gs:hover {
            background-color: #e0a800;
        }

        .gs-logo {
            width: 90px;
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container-fluid">
        <div class="row justify-content-center">
            <div class="col-4">
                <div class="login-box ms-4">
                    <img src="~/images/gslogo.png" alt="GS Logo" class="gs-logo" />
                    <h4>Login</h4>
                    <form method="post">
                        <div class="form-group">
                            <label asp-for="Username"></label>
                            <input asp-for="Username" class="form-control" />
                        </div>
                        <div class="form-group mt-2">
                            <label asp-for="Password"></label>
                            <input asp-for="Password" class="form-control" type="password" />
                        </div>
                        @if (!string.IsNullOrEmpty(Model.ErrorMessage))
                        {
                            <div class="alert alert-danger mt-2">@Model.ErrorMessage</div>
                        }
                        <button type="submit" class="btn btn-gs mt-3">Login</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</body>
</html>
