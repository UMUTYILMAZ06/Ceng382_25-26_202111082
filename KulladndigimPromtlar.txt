şimdi ödev için 2 tane css çizimi 
2 tane css animasyonu olan
temalı login ekranı yapmanı istiyorum arka planda fotoğraf olacak mesela tıklayınca video oynacyacak gibi


yok tek tema olacak ama 2 css çizimi 2 tane css animasyonu olan login ekranı yapmanı istiyorum 


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Animated Themed Login</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: url('background.jpg') no-repeat center center/cover;
            transition: background 0.5s;
        }
        .login-container {
            position: relative;
            background: rgba(0, 0, 0, 0.7);
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.3);
            text-align: center;
            color: white;
            transform: scale(0.8);
            animation: fadeIn 0.8s forwards;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: scale(0.8); }
            to { opacity: 1; transform: scale(1); }
        }
        input {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: none;
            border-radius: 5px;
            background: rgba(255, 255, 255, 0.2);
            color: white;
            transition: all 0.3s ease-in-out;
        }
        input:focus {
            background: rgba(255, 255, 255, 0.5);
            transform: scale(1.05);
        }
        .custom-icon {
            width: 50px;
            height: 50px;
            background: linear-gradient(45deg, #ff6600, #ff3300);
            border-radius: 50%;
            display: inline-block;
            margin-bottom: 20px;
            animation: bounce 1s infinite alternate;
        }
        @keyframes bounce {
            from { transform: translateY(0); }
            to { transform: translateY(-10px); }
        }
        button {
            background: #ff6600;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        button:hover {
            background: #ff3300;
            transform: scale(1.1);
        }
        .video-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            display: none;
        }
        .video-container video {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <div class="custom-icon"></div>
        <h2>Login</h2>
        <input type="text" placeholder="Username">
        <input type="password" placeholder="Password">
        <button onclick="playVideo()">Login</button>
    </div>
    <div class="video-container" id="videoContainer">
        <video src="video.mp4" autoplay controls></video>
    </div>
    <script>
        function playVideo() {
            document.getElementById("videoContainer").style.display = "block";
        }
    </script>
</body>
</html>
animasyon olarak bıçak çzimeni istiyorum css ile login e geklince bıçak sesi çalsın ve çizdiğin bıçaklar ileri doğru uzasın assasin creed gibi bacrounda bu





<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Assassin's Creed Themed Login</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: url('background.jpg') no-repeat center center/cover;
            transition: background 0.5s;
        }
        .login-container {
            position: relative;
            background: rgba(0, 0, 0, 0.7);
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.3);
            text-align: center;
            color: white;
            transform: scale(0.8);
            animation: fadeIn 0.8s forwards;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: scale(0.8); }
            to { opacity: 1; transform: scale(1); }
        }
        input {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: none;
            border-radius: 5px;
            background: rgba(255, 255, 255, 0.2);
            color: white;
            transition: all 0.3s ease-in-out;
        }
        input:focus {
            background: rgba(255, 255, 255, 0.5);
            transform: scale(1.05);
        }
        .custom-icon {
            width: 50px;
            height: 50px;
            background: linear-gradient(45deg, #ff6600, #ff3300);
            border-radius: 50%;
            display: inline-block;
            margin-bottom: 20px;
            animation: bounce 1s infinite alternate;
        }
        @keyframes bounce {
            from { transform: translateY(0); }
            to { transform: translateY(-10px); }
        }
        .blade {
            position: fixed;
            width: 10px;
            height: 100px;
            background: silver;
            border-radius: 5px;
            bottom: -10px;
            transform: scaleY(0);
            transition: transform 0.5s ease-out;
        }
        .blade-left {
            left: -5px;
            transform: rotate(38deg) scaleY(0);
        }
        .blade-right {
            left: calc(100% - 20px);
            transform: rotate(-45deg) scaleY(0);
        }
        .login-container:hover .blade {
            transform: scaleY(1.8);
        }
        .login-container:hover .blade-left {
            transform: rotate(38deg) scaleY(1.8);
        }
        .login-container:hover .blade-right {
            transform: rotate(-45deg) scaleY(1.8);
        }
        button {
            background: #ff6600;
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        button:hover {
            background: #ff3300;
            transform: scale(1.1);
        }
        .video-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            display: none;
        }
        .video-container video {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
    </style>
</head>
<body>
    <div class="login-container" onmouseenter="playSound()">
        <div class="custom-icon"></div>
        <div class="blade blade-left"></div>
        <div class="blade blade-right"></div>
        <h2>Login</h2>
        <input type="text" placeholder="Username">
        <input type="password" placeholder="Password">
        <button onclick="playVideo()">Login</button>
    </div>
    <div class="video-container" id="videoContainer">
        <video src="video.mp4" autoplay controls></video>
    </div>
    <audio id="bladeSound" src="blade.mp3"></audio>
    <script>
        function playVideo() {
            document.getElementById("videoContainer").style.display = "block";
        }
        function playSound() {
            document.getElementById("bladeSound").play();
        }
    </script>
</body>
</html>
kod  ve görünüm böyle şiimdi  login menüsünü assasin konseptine uygun tasarla turuncu top yerine daha güzel bir animasyon yap bıçaklaqrın yeri ve animasyonu kalsın ama



<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Assassin's Creed Themed Login</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: url('background.jpg') no-repeat center center/cover;
            transition: background 0.5s;
        }
        .login-container {
            position: relative;
            background: rgba(0, 0, 0, 0.8);
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(255, 0, 0, 0.5);
            text-align: center;
            color: white;
            transform: scale(0.8);
            animation: fadeIn 0.8s forwards;
            border: 2px solid rgba(255, 0, 0, 0.8);
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: scale(0.8); }
            to { opacity: 1; transform: scale(1); }
        }
        input {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: none;
            border-radius: 5px;
            background: rgba(255, 255, 255, 0.2);
            color: white;
            transition: all 0.3s ease-in-out;
        }
        input:focus {
            background: rgba(255, 255, 255, 0.5);
            transform: scale(1.05);
        }
        .blade {
            position: fixed;
            width: 10px;
            height: 100px;
            background: silver;
            border-radius: 5px;
            bottom: -45px;
            transform: scaleY(0);
            transition: transform 0.5s ease-out;
        }
        .blade-left {
            left: -0px;
            transform: rotate(38deg) scaleY(0);
        }
        .blade-right {
            left: calc(100% - 20px);
            transform: rotate(-45deg) scaleY(0);
        }
        .login-container:hover .blade {
            transform: scaleY(1.8);
        }
        .login-container:hover .blade-left {
            transform: rotate(38deg) scaleY(1.8);
        }
        .login-container:hover .blade-right {
            transform: rotate(-45deg) scaleY(1.8);
        }
        button {
            background: rgba(255, 0, 0, 0.8);
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s ease;
            border: 2px solid rgba(255, 255, 255, 0.5);
        }
        button:hover {
            background: rgba(255, 0, 0, 1);
            transform: scale(1.1);
        }
        .video-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            display: none;
        }
        .video-container video {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
    </style>
</head>
<body>
    <div class="login-container" onmouseenter="playSound()">
        <div class="blade blade-left"></div>
        <div class="blade blade-right"></div>
        <h2>Login</h2>
        <input type="text" placeholder="Username">
        <input type="password" placeholder="Password">
        <button onclick="playVideo()">Login</button>
    </div>
    <div class="video-container" id="videoContainer">
        <video id="video" src="video.mp4" controls></video>
    </div>
    <audio id="bladeSound" src="blade.mp3"></audio>
    <script>
        function playVideo() {
            let videoContainer = document.getElementById("videoContainer");
            let video = document.getElementById("video");
            videoContainer.style.display = "block";
            video.play();
        }
        function playSound() {
            document.getElementById("bladeSound").play();
        }
    </script>
</body>
</html>
bir de bıçakları çizdiğin gibi Shuriken çiz üstteki login yazsının soluna ve sağına logine gelice animasyon olarak dönsünler







<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Assassin's Creed Themed Login</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: url('background.jpg') no-repeat center center/cover;
            transition: background 0.5s;
        }
        .login-container {
            position: relative;
            background: rgba(0, 0, 0, 0.8);
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(255, 0, 0, 0.5);
            text-align: center;
            color: white;
            transform: scale(0.8);
            animation: fadeIn 0.8s forwards;
            border: 2px solid rgba(255, 0, 0, 0.8);
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: scale(0.8); }
            to { opacity: 1; transform: scale(1); }
        }
        input {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: none;
            border-radius: 5px;
            background: rgba(255, 255, 255, 0.2);
            color: white;
            transition: all 0.3s ease-in-out;
        }
        input:focus {
            background: rgba(255, 255, 255, 0.5);
            transform: scale(1.05);
        }
        .blade {
            position: fixed;
            width: 10px;
            height: 100px;
            background: silver;
            border-radius: 5px;
            bottom: -45px;
            transform: scaleY(0);
            transition: transform 0.5s ease-out;
        }
        .blade-left {
            left: -0px;
            transform: rotate(38deg) scaleY(0);
        }
        .blade-right {
            left: calc(100% - 20px);
            transform: rotate(-45deg) scaleY(0);
        }
        .login-container:hover .blade {
            transform: scaleY(1.8);
        }
        .login-container:hover .blade-left {
            transform: rotate(38deg) scaleY(1.8);
        }
        .login-container:hover .blade-right {
            transform: rotate(-45deg) scaleY(1.8);
        }
        .shuriken {
            width: 30px;
            height: 30px;
            position: absolute;
            bottom: -50px;
            left: 50%;
            transform: translateX(-50%) scale(0);
            border-radius: 50%;
            background: linear-gradient(45deg, silver, gray);
            clip-path: polygon(50% 0%, 61% 35%, 98% 35%, 68% 57%, 79% 91%, 50% 70%, 21% 91%, 32% 57%, 2% 35%, 39% 35%);
            animation: none;
            opacity: 0;
        }
        .shuriken-left {
            left: -40px;
        }
        .shuriken-right {
            right: -40px;
        }
        @keyframes shurikenThrow {
            0% {
                bottom: -50px;
                left: 50%;
                transform: translateX(-50%) scale(0);
                opacity: 0;
            }
            50% {
                opacity: 1;
            }
            100% {
                bottom: -10px;
                transform: scale(1) rotate(360deg);
            }
        }
        .login-container:hover .shuriken {
            animation: shurikenThrow 0.5s ease-out forwards, spin 1s linear infinite;
        }
        @keyframes spin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }
        button {
            background: rgba(255, 0, 0, 0.8);
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s ease;
            border: 2px solid rgba(255, 255, 255, 0.5);
        }
        button:hover {
            background: rgba(255, 0, 0, 1);
            transform: scale(1.1);
        }
        .video-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            display: none;
        }
        .video-container video {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
    </style>
</head>
<body>
    <div class="login-container" onmouseenter="playSound()">
        <div class="shuriken shuriken-left"></div>
        <h2>Login</h2>
        <div class="shuriken shuriken-right"></div>
        <div class="blade blade-left"></div>
        <div class="blade blade-right"></div>
        <input type="text" placeholder="Username">
        <input type="password" placeholder="Password">
        <button onclick="playVideo()">Login</button>
    </div>
    <div class="video-container" id="videoContainer">
        <video id="video" src="video.mp4" controls></video>
    </div>
    <audio id="bladeSound" src="blade.mp3"></audio>
    <script>
        function playVideo() {
            let videoContainer = document.getElementById("videoContainer");
            let video = document.getElementById("video");
            videoContainer.style.display = "block";
            video.play();
        }
        function playSound() {
            document.getElementById("bladeSound").play();
        }
    </script>
</body>
</html>
login ekranının sol ve sağ başına gitsinler ve ulaşınca orda kalıp ldukları yerde dönsünler





<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Assassin's Creed Themed Login</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: url('background.jpg') no-repeat center center/cover;
            transition: background 0.5s;
        }
        .login-container {
            position: relative;
            background: rgba(0, 0, 0, 0.8);
            padding: 40px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(255, 0, 0, 0.5);
            text-align: center;
            color: white;
            transform: scale(0.8);
            animation: fadeIn 0.8s forwards;
            border: 2px solid rgba(255, 0, 0, 0.8);
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: scale(0.8); }
            to { opacity: 1; transform: scale(1); }
        }
        input {
            width: 100%;
            padding: 10px;
            margin: 10px 0;
            border: none;
            border-radius: 5px;
            background: rgba(255, 255, 255, 0.2);
            color: white;
            transition: all 0.3s ease-in-out;
        }
        input:focus {
            background: rgba(255, 255, 255, 0.5);
            transform: scale(1.05);
        }
        .blade {
            position: fixed;
            width: 10px;
            height: 100px;
            background: silver;
            border-radius: 5px;
            bottom: -45px;
            transform: scaleY(0);
            transition: transform 0.5s ease-out;
        }
        .blade-left {
            left: -0px;
            transform: rotate(38deg) scaleY(0);
        }
        .blade-right {
            left: calc(100% - 20px);
            transform: rotate(-45deg) scaleY(0);
        }
        .login-container:hover .blade {
            transform: scaleY(1.8);
        }
        .login-container:hover .blade-left {
            transform: rotate(38deg) scaleY(1.8);
        }
        .login-container:hover .blade-right {
            transform: rotate(-45deg) scaleY(1.8);
        }
        .shuriken {
            width: 30px;
            height: 30px;
            position: absolute;
            bottom: -50px;
            left: 50%;
            transform: translateX(-50%) scale(0);
            border-radius: 50%;
            background: linear-gradient(45deg, silver, gray);
            clip-path: polygon(50% 0%, 61% 35%, 98% 35%, 68% 57%, 79% 91%, 50% 70%, 21% 91%, 32% 57%, 2% 35%, 39% 35%);
            animation: none;
            opacity: 0;
        }
        .shuriken-left {
            left: -40px;
        }
        .shuriken-right {
            left: 500px;
        }
        @keyframes shurikenThrow {
            0% {
                bottom: -50px;
                left: 50%;
                transform: translateX(-50%) scale(0);
                opacity: 0;
            }
            50% {
                opacity: 1;
            }
            100% {
                bottom: -10px;
                transform: scale(1) rotate(360deg);
            }
        }
        .login-container:hover .shuriken {
            animation: shurikenThrow 0.5s ease-out forwards, spin 1s linear infinite;
        }
        @keyframes spin {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }
        button {
            background: rgba(255, 0, 0, 0.8);
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: all 0.3s ease;
            border: 2px solid rgba(255, 255, 255, 0.5);
        }
        button:hover {
            background: rgba(255, 0, 0, 1);
            transform: scale(1.1);
        }
        .video-container {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            display: none;
        }
        .video-container video {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }
    </style>
</head>
<body>
    <div class="login-container" onmouseenter="playSound()">
        <div class="shuriken shuriken-left"></div>
        <h2>Login</h2>
        <div class="shuriken shuriken-right"></div>
        <div class="blade blade-left"></div>
        <div class="blade blade-right"></div>
        <input type="text" placeholder="Username">
        <input type="password" placeholder="Password">
        <button onclick="playVideo()">Login</button>
    </div>
    <div class="video-container" id="videoContainer">
        <video id="video" src="video.mp4" controls></video>
    </div>
    <audio id="bladeSound" src="blade.mp3"></audio>
    <script>
        function playVideo() {
            let videoContainer = document.getElementById("videoContainer");
            let video = document.getElementById("video");
            videoContainer.style.display = "block";
            video.play();
        }
        function playSound() {
            document.getElementById("bladeSound").play();
        }
    </script>
</body>
</html>
bunların haricinde loginin sol ve sağında sürekli dönen hançer çiz css ile





/* Hançerlerin ortak stilleri */
.dagger-container {
    position: absolute;
    top: 20px; /* Sayfanın üst kısmına */
    display: flex;
    align-items: center;
}

/* Sol hançer */
.dagger-left {
    left: 40%;
    transform: rotate(-45deg);
}

/* Sağ hançer */
.dagger-right {
    right: 40%;
    transform: rotate(45deg);
}

/* Hançerin bıçak kısmı */
.dagger-blade {
    width: 10px;
    height: 60px;
    background: linear-gradient(180deg, silver, gray);
    clip-path: polygon(50% 0%, 70% 15%, 100% 100%, 0% 100%, 30% 15%);
    border-radius: 2px;
    position: relative;
}

/* Hançerin sapı (artık ayrı bir div) */
.dagger-handle {
    width: 20px; /* Sap genişliği */
    height: 18px; /* Sap uzunluğu */
    background: black;
    position: absolute;
    top: -18px; /* Bıçağın üstüne ekleme */
    left: 50%;
    transform: translateX(-50%);
    border-radius: 4px;
}  siyah yeri bıçağın diğer ucuna al



biri farklı biri farklı yöne istiyorum


assasincreed logosunu css koduyla çiz



 .ac-logo {
            position: absolute;
            top: 15%;  /* Sayfanın üst kısmına yerleştir */
            left: 20%;
            transform: translateX(-50%);
            width: 100px;
            height: 120px;
        }

        /* Üstteki A harfi şeklindeki üçgen */
        .ac-logo-top {
            width: 0;
            height: 0;
            border-left: 50px solid transparent;
            border-right: 50px solid transparent;
            border-bottom: 100px solid black;
            position: absolute;
            top: 0;
            left: 0;
        }

        /* Alt kısımdaki kanatları oluşturan şekil */
        .ac-logo-bottom {
            width: 100px;
            height: 50px;
            background: black;
            position: absolute;
            bottom: 0;
            left: 0;
            clip-path: polygon(0% 100%, 15% 35%, 50% 0%, 85% 35%, 100% 100%, 50% 70%);
        }
bundan bir tahaned daha yap o da sağala doğru olsun



    /* Assassin's Creed Logosu için Ana Konteyner */
.ac-logo {
    position: absolute;
    top: 15%;  /* Sayfanın üst kısmına yerleştir */
    width: 100px;
    height: 120px;
}

/* Sol Logo */
.ac-logo-left {
    left: 20%; /* Sol tarafta */
    transform: translateX(-50%);
}

/* Sağ Logo */
.ac-logo-right {
    left: 80%; /* Sağ tarafta */
    transform: translateX(-50%);
}

/* Üstteki A harfi şeklindeki üçgen */
.ac-logo-top {
    width: 0;
    height: 0;
    border-left: 50px solid transparent;
    border-right: 50px solid transparent;
    border-bottom: 100px solid black;
    position: absolute;
    top: 0;
    left: 0;
}

/* Alt kısımdaki kanatları oluşturan şekil */
.ac-logo-bottom {
    width: 100px;
    height: 50px;
    background: black;
    position: absolute;
    bottom: 0;
    left: 0;
    clip-path: polygon(0% 100%, 15% 35%, 50% 0%, 85% 35%, 100% 100%, 50% 70%);
}
bunları login e hoover yapınca ortaya çıkart

